# GitHub workflow
## prequisite
You have a Github repo.

## build your own workflow for GitHub repo
Define your workflow on a file with yaml format (`.yml` or `.yaml` extension).

Then commit and push it under `~/.github/workflows` directory

where 

`~` is the root directory of your GitHub repo.

